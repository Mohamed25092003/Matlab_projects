function[signal]=exp_generate(A,k,t)
           % A = input('Enter the amplitude of the exponential signal: ');
           % k = input('Enter the exponent of the exponential signal: ');
            signal= A*exp(k*t);
end